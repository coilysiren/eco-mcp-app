{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "(lsof -iTCP:4000 -sTCP:LISTEN -P 2>/dev/null | grep -q LISTEN) || (DEBUG=1 nohup uv run uvicorn eco_mcp_app.http_app:app --reload --host 0.0.0.0 --port 4000 >/tmp/eco-mcp-http.log 2>&1 &)",
            "async": true,
            "timeout": 10
          }
        ]
      }
    ]
  }
}
